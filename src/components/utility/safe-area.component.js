import { SafeAreaView, StatusBar } from 'react-native'
import styled from 'styled-components/native'

/* ${ StatusBar.currentHeight && `marginTop:${StatusBar.currentHeight}` } */
export const SafArea = styled(SafeAreaView)`
  flex:1;
 
`